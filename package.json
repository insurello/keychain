{
  "name": "keychain",
  "version": "2.0.0",
  "private": true,
  "description": "Manage JWK key sets and issue/verify JWT tokens using public key signatures.",
  "license": "MIT",
  "author": {
    "name": "Insurello AB"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/*.js",
    "dist/*.d.ts"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc --project . --pretty --declaration",
    "lint": "prettier --check . && tslint '{src,test}/**/*.ts' --format codeFrame",
    "test": "jest"
  },
  "dependencies": {
    "jwk-to-pem": "^2.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/jsonwebtoken": "^8.5.0",
    "fp-ts": "^2.8.3",
    "io-ts": "^2.2.10",
    "jest": "^26.4.2",
    "jsonwebtoken": "^8.3.0",
    "prettier": "^2.1.2",
    "ts-jest": "^26.4.1",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "tslint-insurello": "github:insurello/tslint-insurello#v2.2.1",
    "typescript": "^4.0.3"
  },
  "peerDependencies": {
    "fp-ts": "2.x",
    "io-ts": "2.x",
    "jsonwebtoken": "8.x"
  }
}
